<% provide(:title, 'New list') %>
<% provide(:button_text, 'Create list') %>

<% if (defined? @table_id) && @table_id.present? %>
  <%= render partial: 'form', locals: {table_ids: [@table_id]} %>
<% else %>
  <%= render partial: 'form' %>
<% end %>

